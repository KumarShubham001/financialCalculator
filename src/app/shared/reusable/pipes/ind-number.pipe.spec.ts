import { IndNumberPipe } from './ind-number.pipe';

describe('IndNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new IndNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
